# GradeOS Platform

> 统一的 AI 驱动教育平台，深度融合四大核心系统

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Python 3.11+](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://python.org)
[![Next.js 15](https://img.shields.io/badge/Next.js-15-black.svg)](https://nextjs.org)

## 📋 目录

- [项目概述](#项目概述)
- [系统架构](#系统架构)
- [目录结构](#目录结构)
- [快速开始](#快速开始)
- [功能模块](#功能模块)
- [API 文档](#api-文档)
- [数据库设计](#数据库设计)
- [部署指南](#部署指南)
- [开发指南](#开发指南)

---

## 项目概述

GradeOS Platform 整合了四个独立的教育应用系统：

| 原项目 | 功能 | 整合位置 |
|--------|------|----------|
| `批改/` | AI 视觉批改引擎 | `backend/` + `frontend/console/` |
| `GradeOS-frontend/` | 教师管理平台 | `frontend/teacher/` |
| `studentassisant/` | 学生 AI 助手 | `frontend/student/assistant/` |
| `intellilearn---ai-teaching-agent/` | 错题分析系统 | `frontend/student/analysis/` + `frontend/student/report/` |

### 核心特性

- 🤖 **AI 批改引擎** - 视觉原生 PDF 处理，多智能体批改流水线
- 📚 **班级管理** - 创建班级、邀请码、学生管理
- 📝 **作业系统** - 发布、提交、自动批改、成绩统计
- 🔍 **错题分析** - 深度错因诊断、知识漏洞识别
- 📈 **学情报告** - 成长曲线、能力雷达、个性化洞察
- 🎯 **AI 学习助手** - 智能对话、学习规划

---

## 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Frontend (Next.js 15)                     │
├─────────────┬─────────────┬─────────────┬─────────────┬─────────┤
│   Login     │   Teacher   │   Student   │   Console   │  共享   │
│   Page      │   Module    │   Module    │   (AI批改)  │  组件   │
└──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┴────┬────┘
       │             │             │             │           │
       └─────────────┴─────────────┴─────────────┴───────────┘
                                   │
                          ┌────────▼────────┐
                          │   API Gateway   │
                          │  (FastAPI)      │
                          └────────┬────────┘
                                   │
       ┌───────────────────────────┼───────────────────────────┐
       │                           │                           │
┌──────▼──────┐  ┌─────────────────▼─────────────────┐  ┌──────▼──────┐
│  统一 API   │  │      AI 批改引擎 (LangGraph)      │  │   缓存层    │
│  Routes     │  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ │  │   (Redis)   │
│             │  │  │OCR  │→│解析 │→│批改 │→│汇总 │ │  │             │
└──────┬──────┘  │  └─────┘ └─────┘ └─────┘ └─────┘ │  └──────┬──────┘
       │         └─────────────────┬─────────────────┘         │
       │                           │                           │
       └───────────────────────────┼───────────────────────────┘
                                   │
                          ┌────────▼────────┐
                          │   PostgreSQL    │
                          │   (19 Tables)   │
                          └─────────────────┘
```

---

## 目录结构

```
GradeOS-Platform/
│
├── backend/                          # Python FastAPI 后端
│   ├── src/
│   │   ├── api/
│   │   │   ├── routes/
│   │   │   │   ├── unified_api.py    # ⭐ 统一 API 路由 (班级/作业/错题分析)
│   │   │   │   ├── submissions.py    # 提交管理
│   │   │   │   ├── rubrics.py        # 评分标准
│   │   │   │   ├── reviews.py        # 人工审核
│   │   │   │   └── batch.py          # 批量批改
│   │   │   ├── middleware/           # 中间件 (限流等)
│   │   │   ├── main.py               # FastAPI 入口
│   │   │   └── dependencies.py       # 依赖注入
│   │   │
│   │   ├── agents/                   # AI 智能体
│   │   │   ├── grading_agent.py      # 批改智能体
│   │   │   ├── supervisor.py         # 监督智能体
│   │   │   └── pool.py               # 智能体池
│   │   │
│   │   ├── services/                 # 业务服务层
│   │   │   ├── rubric_parser.py      # 评分标准解析
│   │   │   ├── strict_grading.py     # 严格批改服务
│   │   │   ├── student_identification.py  # 学生识别
│   │   │   ├── cache.py              # 缓存服务
│   │   │   └── ...
│   │   │
│   │   ├── models/                   # 数据模型
│   │   │   ├── unified_models.py     # ⭐ 统一数据模型 (19表)
│   │   │   ├── submission.py         # 提交模型
│   │   │   └── ...
│   │   │
│   │   ├── graphs/                   # LangGraph 工作流
│   │   │   ├── batch_grading.py      # 批量批改图
│   │   │   └── state.py              # 状态定义
│   │   │
│   │   ├── repositories/             # 数据访问层
│   │   ├── utils/                    # 工具函数
│   │   │   ├── database.py           # 数据库连接
│   │   │   └── pool_manager.py       # 连接池管理
│   │   │
│   │   └── workers/                  # 后台工作进程
│   │
│   ├── scripts/
│   │   └── init_database.sql         # ⭐ 数据库初始化脚本
│   │
│   ├── tests/                        # 测试套件
│   ├── requirements.txt              # Python 依赖
│   └── .env.example                  # 环境变量模板
│
├── frontend/                         # Next.js 15 前端
│   ├── src/
│   │   ├── app/
│   │   │   ├── (auth)/
│   │   │   │   └── login/page.tsx    # 登录页面
│   │   │   │
│   │   │   ├── teacher/              # 教师模块 (来自 GradeOS-frontend)
│   │   │   │   ├── dashboard/page.tsx    # 班级管理
│   │   │   │   ├── homework/page.tsx     # 作业管理
│   │   │   │   ├── statistics/page.tsx   # 数据统计
│   │   │   │   └── class/[id]/page.tsx   # 班级详情
│   │   │   │
│   │   │   ├── student/              # 学生模块
│   │   │   │   ├── dashboard/page.tsx    # 我的课程
│   │   │   │   ├── assistant/page.tsx    # AI 助手 (来自 studentassisant)
│   │   │   │   ├── analysis/page.tsx     # 错题分析 (来自 intellilearn)
│   │   │   │   └── report/page.tsx       # 学情报告 (来自 intellilearn)
│   │   │   │
│   │   │   ├── console/page.tsx      # AI 批改控制台 (来自 批改)
│   │   │   ├── layout.tsx
│   │   │   └── page.tsx              # 首页
│   │   │
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   └── DashboardLayout.tsx   # ⭐ 统一布局 (角色切换导航)
│   │   │   ├── console/              # 批改控制台组件
│   │   │   └── ...
│   │   │
│   │   ├── services/
│   │   │   └── api.ts                # ⭐ 统一 API 服务层
│   │   │
│   │   ├── store/
│   │   │   ├── authStore.ts          # 认证状态
│   │   │   └── consoleStore.ts       # 控制台状态
│   │   │
│   │   └── types/
│   │       └── index.ts              # TypeScript 类型定义
│   │
│   ├── package.json
│   └── next.config.js
│
├── docker-compose.yml                # Docker 编排
├── start_dev.ps1                     # Windows 开发启动脚本
└── README.md                         # 本文档
```


---

## 快速开始

### 环境要求

| 组件 | 版本 | 说明 |
|------|------|------|
| Node.js | 18+ | 前端运行时 |
| Python | 3.11+ | 后端运行时 |
| PostgreSQL | 15+ | 主数据库 |
| Redis | 7+ | 缓存数据库 |

### 1. 克隆项目

```bash
git clone <repository-url>
cd GradeOS-Platform
```

### 2. 数据库初始化

```bash
# 创建数据库
createdb ai_grading

# 执行初始化脚本
psql -d ai_grading -f backend/scripts/init_database.sql
```

### 3. 后端配置

```bash
cd backend

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
# 或使用 uv
uv sync

# 配置环境变量
cp .env.example .env
# 编辑 .env 设置以下变量:
# DATABASE_URL=postgresql://user:pass@localhost:5432/ai_grading
# REDIS_URL=redis://localhost:6379
# GEMINI_API_KEY=your_api_key
```

### 4. 前端配置

```bash
cd frontend

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env.local
# 设置 NEXT_PUBLIC_API_URL=http://localhost:8001
```

### 5. 启动服务

**方式一：分别启动**

```bash
# 终端 1 - 后端
cd backend
uvicorn src.api.main:app --reload --port 8001

# 终端 2 - 前端
cd frontend
npm run dev
```

**方式二：使用启动脚本 (Windows)**

```powershell
.\start_dev.ps1
```

**方式三：使用 Docker**

```bash
docker-compose up -d
```

### 6. 访问应用

| 服务 | 地址 |
|------|------|
| 前端应用 | http://localhost:3000 |
| 后端 API | http://localhost:8001 |
| API 文档 | http://localhost:8001/docs |

### 演示账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 教师 | teacher | 123456 |
| 学生 | student | 123456 |

---

## 功能模块

### 教师端功能

| 模块 | 路由 | 功能 |
|------|------|------|
| 班级管理 | `/teacher/dashboard` | 创建班级、生成邀请码、管理学生 |
| 作业管理 | `/teacher/homework` | 发布作业、设置截止日期、查看提交 |
| AI 批改 | `/console` | 上传试卷、自动批改、生成反馈 |
| 数据统计 | `/teacher/statistics` | 成绩分布、错误分析、教学建议 |

### 学生端功能

| 模块 | 路由 | 功能 |
|------|------|------|
| 我的课程 | `/student/dashboard` | 加入班级、查看作业、提交答案 |
| AI 学习助手 | `/student/assistant` | 智能对话、学习规划、成绩分析 |
| 错题分析 | `/student/analysis` | 深度错因诊断、知识漏洞识别 |
| 学情报告 | `/student/report` | 成长曲线、能力雷达、个性化洞察 |

---

## API 文档

### 认证接口

```
POST /api/auth/login          # 用户登录
GET  /api/user/info           # 获取用户信息
```

### 班级管理

```
GET  /api/class/my            # 获取学生班级
POST /api/class/join          # 加入班级
GET  /api/teacher/classes     # 获取教师班级
POST /api/teacher/classes     # 创建班级
GET  /api/class/students      # 班级学生列表
```

### 作业管理

```
GET  /api/homework/list       # 作业列表
GET  /api/homework/detail/:id # 作业详情
POST /api/homework/create     # 创建作业
POST /api/homework/submit     # 提交作业
GET  /api/homework/submissions # 提交列表
```

### 错题分析

```
POST /api/v1/analysis/submit-error    # 提交错题分析
GET  /api/v1/diagnosis/report/:id     # 获取诊断报告
GET  /api/v1/class/wrong-problems     # 班级高频错题
```

### 统计分析

```
GET  /api/teacher/statistics/class/:id  # 班级统计
POST /api/teacher/statistics/merge      # 合并外部数据
```

### AI 批改

```
POST /submissions/upload      # 上传试卷
GET  /submissions/:id         # 查询状态
POST /batch/grade-cached      # 批量批改
WS   /batch/ws/:batch_id      # 实时进度
```

---

## 数据库设计

系统包含 19 张核心数据表，详见 `后端数据库需求文档_基于API整合.md`

| 模块 | 表名 | 说明 |
|------|------|------|
| 用户认证 | users, user_profiles | 用户账号与配置 |
| 班级管理 | classes, student_class_relations | 班级与学生关系 |
| 作业管理 | assignments, assignment_submissions | 作业与提交 |
| AI 批改 | grading_tasks, grading_results | 批改任务与结果 |
| 错题分析 | error_records, learning_recommendations | 错题与建议 |
| 知识图谱 | knowledge_points, student_knowledge_mastery | 知识点掌握 |
| 统计分析 | class_statistics, system_logs | 统计与日志 |

---

## 部署指南

### Docker 部署

```yaml
# docker-compose.yml
version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: ai_grading
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/scripts/init_database.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

  backend:
    build: ./backend
    ports:
      - "8001:8001"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/ai_grading
      REDIS_URL: redis://redis:6379
    depends_on:
      - postgres
      - redis

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://backend:8001
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
```

### 生产环境配置

1. **数据库**: 使用 PostgreSQL 主从复制
2. **缓存**: Redis 集群 (3主3从)
3. **负载均衡**: Nginx 反向代理
4. **SSL**: Let's Encrypt 证书
5. **监控**: Prometheus + Grafana

---

## 开发指南

### 代码规范

- **Python**: Black + isort + flake8
- **TypeScript**: ESLint + Prettier
- **提交信息**: Conventional Commits

### 分支策略

```
main          # 生产分支
├── develop   # 开发分支
│   ├── feature/xxx   # 功能分支
│   └── bugfix/xxx    # 修复分支
└── release/x.x.x     # 发布分支
```

### 测试

```bash
# 后端测试
cd backend
pytest tests/ -v

# 前端测试
cd frontend
npm test
```

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Next.js 15, React 19, Tailwind CSS, Zustand, Recharts |
| 后端 | FastAPI, LangGraph, Pydantic |
| 数据库 | PostgreSQL 15, Redis 7 |
| AI | Google Gemini 2.5 Flash |
| 部署 | Docker, Kubernetes |

---

## License

MIT License - 详见 [LICENSE](LICENSE)
